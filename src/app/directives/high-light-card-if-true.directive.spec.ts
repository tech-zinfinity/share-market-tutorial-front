import { HighLightCardIfTrueDirective } from './high-light-card-if-true.directive';

describe('HighLightCardIfTrueDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightCardIfTrueDirective();
    expect(directive).toBeTruthy();
  });
});
